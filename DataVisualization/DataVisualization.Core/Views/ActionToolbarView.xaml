<UserControl x:Class="DataVisualization.Core.Views.ActionToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataVisualization.Core.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ToolBarTray>
        <ToolBar Style="{DynamicResource MaterialDesignToolBar}" ClipToBounds="False">
            <Button ToolTip="Run" cal:Message.Attach="[Event Click] = [Action OnRunCode()]">
                <materialDesign:PackIcon Kind="Play" />
            </Button>
            <Button ToolTip="Stop" cal:Message.Attach="[Event Click] = [Action OnStopCodeExecution()]">
                <materialDesign:PackIcon Kind="Stop" />
            </Button>
            <Separator />
        </ToolBar>
    </ToolBarTray>
</UserControl>
