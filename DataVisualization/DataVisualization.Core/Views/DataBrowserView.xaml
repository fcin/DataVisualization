<UserControl x:Class="DataVisualization.Core.Views.DataBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataVisualization.Core.Views"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200" MinWidth="100">
    <Grid>
        <StackPanel>
            <Label Content="All configurations"></Label>
            <ListView Name="DataConfigs" ItemsSource="{Binding AllDataConfigurations}" MouseDoubleClick="OpenConfig">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Label Content="{Binding DataName}"></Label>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
