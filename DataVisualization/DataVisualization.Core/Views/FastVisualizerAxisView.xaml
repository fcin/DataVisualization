<hx:ItemsModel3D x:Class="DataVisualization.Core.Views.FastVisualizerAxisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataVisualization.Core.Views"
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             mc:Ignorable="d">
    <hx:LineGeometryModel3D Geometry="{Binding Model}"
                            Transform="{Binding Transform}"
                            Color="{Binding Color}"/>
    <hx:ItemsModel3D ItemsSource="{Binding LabelVms}">
        <hx:ItemsModel3D.ItemTemplate>
            <DataTemplate>
                <local:FastVisualizerLabelView></local:FastVisualizerLabelView>
            </DataTemplate>
        </hx:ItemsModel3D.ItemTemplate>
    </hx:ItemsModel3D>
</hx:ItemsModel3D>